<Window x:Class="AutoScreenshot.Views.ManualTitleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="手順書セッション開始" Height="148" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="手順書タイトルを入力してください:" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="（空欄の場合: 操作手順書 YYYY-MM-DD HH:mm）"
                   FontSize="10" Foreground="Gray" Margin="0,0,0,8"/>
        <TextBox x:Name="TxtTitle" Grid.Row="2"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="75" Margin="5,0" IsDefault="True" Click="BtnOk_Click"/>
            <Button Content="キャンセル" Width="75" Margin="5,0" IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
